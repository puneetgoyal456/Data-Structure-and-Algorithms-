#include<iostream>
using namespace std;

int main(){

    // int age;
    // cin >> age;

    // (age>=18) ? cout << "Can vote" : cout << "Cannot Vote" << endl;

    int num;
    cin >> num;

    (num%2==0) ? cout << "Even" : cout << "Odd" << endl;



    return 0;
}